# Changelog

## 2.0.0
- Merged object type base classes and built-in ObjectTypes (flow/logic/time/random) into a single package.
- Added `arcor2_upload_object_types` upload script for seeding built-ins into `arcor2_storage`.
